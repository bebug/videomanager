<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <link href='http://fonts.googleapis.com/css?family=VT323' rel='stylesheet' type='text/css'>
    <title>{{ status_text }}</title>
    <style type="text/css">
    html, body{
    	font-family: 'VT323', cursive;
    	color:#dddddd;
    	background-color:#3D79D0;
    	font-size:1.2em;
    }
    </style>
</head>
<body>
    <h1>Oops! Es gab einen Fehler</h1>
    <h2>Statuscode "{{ status_code }} {{ status_text }}".</h2>
    {% if is_granted('ROLE_USER') %}{{ exception.message }}{% else %}Wir werden den Fehler so schnell wie möglich beheben.{% endif %}
    <div id="blink" style="display: inline;"></div>
    
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js"></script>
    
    <script type="text/javascript">
    $(document).ready(function() {
    	setBlock();
	});

    function setBlock()
    {
    	document.getElementById("blink").innerHTML='<strong>_</strong>';
    	setTimeout(function(){deleteBlock()},300);
    }

    function deleteBlock()
    {
    	document.getElementById("blink").innerHTML='';
    	setTimeout(function(){setBlock()},300);
    }
    
	</script>
</body>
</html>